
<div class=" w-full h-full flex justify-center items-center">
<%= render(BoardComponent.new(board: @board)) %>


</div>
<a href="#"
   data-reflex="click->Test#increment"
   data-step="1"
   data-count="<%= @count.to_i %>">
  Increment <%= @count.to_i %>
  <%= @message %>
</a>
<%= render(ReflexComponent.new) %>

<button type="button" data-reflex="click->reflexComponent#test">Click me!</button>